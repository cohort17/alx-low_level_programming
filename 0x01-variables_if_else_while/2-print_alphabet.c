#include <stdio.h>
/**
*main- starting code execution point
*
*Return: value after code execution done
*/
int main(void)
{
int v;
for (v = 97; v <= 122; v++)
{
putchar(v);
}
putchar('\n');
return (0);
}
