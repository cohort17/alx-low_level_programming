#include <stdio.h>
/**
*main- starting code execution point
*
*Return: value after code execution done
*/
int main(void)
{
int a;
for (a = 0; a <= 9; a++)
{
putchar('0' + a);
}
putchar('\n');
return (0);
}
