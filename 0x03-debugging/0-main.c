#include "main.h"
/**
*main- tests function that prints if integer is positive or negative
*@i: it is the number to be checked
*Return: it is value is 0 if successful
*/
void positive_or_negative(int i)
{
i = 0;
positive_or_negative(i);
return (0);
}
